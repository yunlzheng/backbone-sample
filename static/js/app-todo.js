 // js/app.js

var app = app || {};
var ENTER_KEY = 13;

$(function() {

    // Kick things off by creating the **App**.
    console.log('init application');
    application = new app.AppView();
    console.log("init application done!");

});
